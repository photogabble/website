<figure class="citation">
    {{ content }}
    {%- if cite -%}
        <figcaption>{%- if cite.href -%}
            <span>&mdash; <a href="{{ cite.href }}">{{ cite.name }}</a> | <a href="{{ page.url }}">notes</a></span>
        {%- else -%}
            <span>&mdash; {{ cite.name }} | <a href="{{ page.url }}">my notes</a></span>
        {%- endif -%}
        </figcaption>
    {%- endif -%}
</figure>
