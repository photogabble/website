{% for tag in tags | withoutTags(['writing']) | excludeSpecialTags(['type', 'resource']) | formatTagList %}<a href="{{ tag.permalink }}" title="Visit {{ tag.title }} index">{{ tag.title }}</a>{{ ' and ' if loop.revindex == 2 }}{{ ', ' if loop.revindex > 2 }}{% endfor %}
