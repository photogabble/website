const chunk = require('./chunk');
const strToSlug = require('./strToSlug');
const toTitleCase = require('./toTitleCase');
const backlinks = require('./backlinks');

module.exports = {
    chunk,
    strToSlug,
    backlinks,
    toTitleCase
}