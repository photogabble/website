<ol class="post-list">
    {% for post in postsList %}
        <li>
            <span>
                <a href="{{ post.url }}" title="Read {{ post.data.title }}">{{ post.data.title }}</a>
                {% if displayContentType %}
                    <small class="content-type">{{ post.data.contentType }}</small>
                {% endif %}
            </span>
            <hr/>
            <small>{{ post.date | dateToFormat("DDD") }}</small>
        </li>
    {% endfor %}
</ol>