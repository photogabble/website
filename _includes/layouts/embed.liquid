<aside>
    {% block content %}{% endblock %}
    {% if cite.href %}
        <small>&mdash; <a href="{{ cite.href }}">{{ cite.name }}</a> | <a href="{{ page.url }}">link</a></small>
    {% else %}
        <small>&mdash; {{ cite.name }} | <a href="{{ page.url }}">link</a></small>
    {% endif %}
</aside>